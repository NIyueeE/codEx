From 57fd4cd325493ab7d1ff52887a6fa9466a9def42 Mon Sep 17 00:00:00 2001
From: codEx Fork Bot <codex-fork-bot@localhost>
Date: Tue, 11 Aug 2026 01:20:45 +0800
Subject: branding: codEx name and version output

codex --version prints "codEx <version> (codEx fork)" while keeping plain
semver so fork release tags stay clean, and the sandboxed version-command
test accepts the capitalized fork name.
---
 README.md                                     | 110 ++++--
 codex-rs/cli/src/main.rs                      |  25 +-
 codex-rs/cli/tests/sandbox_cloud_config.rs    |   4 +-
 .../src/event_processor_with_human_output.rs  |   2 +-
 codex-rs/login/src/device_code_auth.rs        |   4 +-
 codex-rs/login/src/device_code_auth_tests.rs  |   2 +-
 codex-rs/tui/src/app/event_dispatch.rs        |  22 +-
 codex-rs/tui/src/app/tests.rs                 | 338 +++++++++++++++++-
 .../app/tests/session_lifecycle_requests.rs   |   4 +-
 codex-rs/tui/src/app_backtrack.rs             |  78 +++-
 codex-rs/tui/src/history_cell/session.rs      |   6 +-
 ...session_header_clamps_to_narrow_width.snap |   2 +-
 ...s__session_header_halfwidth_directory.snap |   4 +-
 ..._session_header_halfwidth_sound_marks.snap |   4 +-
 ...s__session_header_indicates_yolo_mode.snap |   4 +-
 ...nfo_availability_nux_tooltip_snapshot.snap |   2 +-
 codex-rs/tui/src/onboarding/welcome.rs        |   4 +-
 codex-rs/tui/src/rewind/actions.rs            |  78 +---
 ...ter_long_transcript_fresh_header_only.snap |   4 +-
 ...eader_fast_status_fast_capable_models.snap |   4 +-
 codex-rs/tui/src/status/card.rs               |   2 +-
 ...ched_limits_hide_credits_without_flag.snap |   2 +-
 ..._snapshot_includes_credits_and_limits.snap |   2 +-
 ...ludes_enterprise_monthly_credit_limit.snap |   2 +-
 ..._status_snapshot_includes_forked_from.snap |   2 +-
 ...tatus_snapshot_includes_monthly_limit.snap |   2 +-
 ...s_snapshot_includes_reasoning_details.snap |   2 +-
 ...hot_shows_active_user_defined_profile.snap |   2 +-
 ...napshot_shows_auto_review_permissions.snap |   2 +-
 ...shot_shows_chatgpt_plan_without_email.snap |   2 +-
 ...snapshot_shows_missing_limits_message.snap |   2 +-
 ...apshot_shows_refreshing_limits_notice.snap |   2 +-
 ...s_snapshot_shows_stale_limits_message.snap |   2 +-
 ...shot_shows_unavailable_limits_message.snap |   2 +-
 ...efreshing_empty_limits_as_unavailable.snap |   2 +-
 ...tes_halfwidth_kana_in_narrow_terminal.snap |   2 +-
 ...snapshot_truncates_in_narrow_terminal.snap |   2 +-
 ...s_default_reasoning_when_config_empty.snap |   2 +-
 ..._limit_labels_for_unsupported_windows.snap |   2 +-
 ...hly_credit_details_in_narrow_terminal.snap |   2 +-
 ...ipt_overlay_status_rate_limit_refresh.snap |   4 +-
 codex-rs/tui/src/tooltips.rs                  |   2 +-
 codex-rs/tui/tests/suite/focus_palette.rs     |   2 +-
 43 files changed, 579 insertions(+), 170 deletions(-)

diff --git a/README.md b/README.md
index c518fa7..c6870b5 100644
--- a/README.md
+++ b/README.md
@@ -5,14 +5,52 @@ that keeps the CLI happy-path for standalone Linux users: fewer moving parts,
 no phone-home update checks, and a built-in `codex update` that downloads
 verified binaries from this fork's releases.
 
-This repository follows a **patch-queue model**: it stores only the fork's
-changes (as `git format-patch` series) plus the scripts that rebuild a full
-codex tree from an upstream tag. The actual code lives upstream; `BASE_TAG`
-pins the upstream tag the current patch queue applies to (currently
-`rust-v0.147.0`).
+This repository follows a **patch-queue model**: instead of vendoring the
+upstream tree, it stores only the fork's changes as a `git format-patch`
+series plus the scripts that rebuild a full codex tree from an upstream tag.
+The actual code lives upstream; `BASE_TAG` pins the upstream tag the current
+patch queue applies to (currently `rust-v0.147.0`).
+
+## Installing from a release
+
+Standalone Linux users can install or update codEx with the installer:
+
+```sh
+curl -fsSL https://raw.githubusercontent.com/NIyueeE/codEx/main/install.sh | sh
+```
+
+The installer resolves the latest fork release, downloads
+`codex-<target>.tar.gz` plus its published sha256 checksum, verifies the
+checksum before touching anything, and installs into the standalone layout
+under `~/.codex/packages/standalone/releases/<version>-<target>/` (with the
+bundled `bwrap` under `codex-resources/`) so `codex update` keeps working
+afterwards. It then links `codex` into `~/.local/bin` (or `CODEX_INSTALL_DIR`).
+
+Environment overrides:
+
+- `CODEX_RELEASE` - version to install, e.g. `0.147.0` (default: `latest`)
+- `CODEX_INSTALL_DIR` - directory for the `codex` symlink (default: `~/.local/bin`)
+- `CODEX_HOME` - codex home (default: `~/.codex`)
 
 ## What changes versus upstream
 
+codEx keeps the upstream `codex` binary name and configuration format, so it
+drops into existing workflows, but changes the surrounding experience:
+
+- **Rollback**: `/rewind` undoes conversation turns and/or workspace file
+  changes without git.
+- **Input**: Esc no longer interrupts by accident; a double press stops a
+  running reply.
+- **Updates**: `codex update` is a pure-Rust downloader with checksum
+  verification; no `curl | sh`, npm, or brew.
+- **Privacy**: no update checks or announcement fetches on startup by default.
+- **Distribution**: Linux-only standalone archives (`codex` + `bwrap`), no
+  macOS/Windows/app-server bundles.
+- **Identity**: the CLI and TUI brand themselves as codEx, and
+  `codex --version` points at this fork's repository.
+
+Details below.
+
 ### `/rewind` - roll back conversation and workspace files
 
 The headline feature. `/rewind` lets you undo a conversation turn and/or the
@@ -20,14 +58,19 @@ file changes a turn made, without needing git.
 
 **Usage**: type `/rewind`, then choose a scope:
 
-1. **Files and conversation** - pick a past message; the chat forks before that
-   message (a new thread with the conversation truncated there, prompt
-   restored into the composer) and the workspace files are restored to the
-   snapshot taken right before that message.
-2. **Conversation only** - pick a past message from a searchable picker and
-   fork the chat before it; files are left untouched.
+1. **Files and conversation** - pick a past message in the transcript; the chat
+   forks before that message (a new thread with the conversation truncated
+   there, prompt restored into the composer) and the workspace files are
+   restored to the snapshot taken right before that message.
+2. **Conversation only** - pick a past message in the transcript and fork the
+   chat before it; files are left untouched.
 3. **Files only** - pick a numbered snapshot and restore the workspace to it.
 
+Both conversation scopes reuse the transcript picker (the same full-history
+view the old double-Esc backtrack used): the newest user message starts
+highlighted, **Esc** or **Left** steps to older messages, **Right** steps
+newer, and **Enter** confirms the rewind.
+
 **How snapshots work** (pure files, no git involved):
 
 - Before each user turn is submitted, the TUI snapshots the workspace into
@@ -61,7 +104,8 @@ rewind_respect_gitignore = true  # default true; false snapshots every file
   the request-user-input panel all still use Esc as before.
 - The `chat.interrupt_turn` keybinding is **unbound by default** (you can bind
   it again in `/keymap`; modal views honor it), and the old "Esc-Esc to edit
-  previous message" backtrack was removed in favor of `/rewind`.
+  previous message" backtrack was removed from the main UI; `/rewind` now
+  reuses its transcript picker for message selection.
 
 ### `codex update`: pure-Rust self-update
 
@@ -91,8 +135,11 @@ rewind_respect_gitignore = true  # default true; false snapshots every file
 
 ### Branding
 
-- `codex --version` prints `codEx 0.147.0 (codEx fork)`; the status bar shows
-  `codEx <version>`.
+- `codex --version` prints
+  `codEx 0.147.0 (codEx fork, https://github.com/NIyueeE/codEx)`; the status
+  bar shows `codEx <version>`.
+- The TUI welcome screen, session header, and status header display `codEx`;
+  tips reference `codEx` as well.
 - The version number itself **matches the upstream base tag** (e.g. `0.147.0`),
   so version parsing stays plain semver and fork release tags stay clean.
 
@@ -114,13 +161,17 @@ rewind_respect_gitignore = true  # default true; false snapshots every file
 
 ### Patch-queue model
 
-This repo deliberately does **not** vendor upstream code. It stores:
+This repo deliberately does **not** vendor upstream code, unlike a classic
+fork that carries the full tree and periodically merges upstream. Instead it
+stores only the delta:
 
-- `BASE_TAG` - the upstream tag the current patch queue applies to
-- `patches/` - `git format-patch` series with every fork change
-- `scripts/bootstrap.sh` - shallow-clone `openai/codex@BASE_TAG`, apply patches
-- `scripts/update.sh` - upgrade to a new upstream tag (apply, resolve, regen)
-- `scripts/gen-patches.sh` - regenerate the patch series from git history
+| Piece | Purpose |
+| --- | --- |
+| `BASE_TAG` | upstream tag the patch queue applies to (e.g. `rust-v0.147.0`) |
+| `patches/` | `git format-patch` series with every fork change |
+| `scripts/bootstrap.sh` | shallow-clone `openai/codex@BASE_TAG` and `git am` the patches |
+| `scripts/update.sh` | upgrade to a new upstream tag (apply, resolve conflicts, regen) |
+| `scripts/gen-patches.sh` | regenerate `patches/` from the bootstrapped tree's git history |
 
 `bootstrap.sh` + `git am` produce the complete, buildable codEx tree; the CI
 and release workflows do exactly that before building. See
@@ -129,19 +180,20 @@ and release workflows do exactly that before building. See
 
 ## Building from this repository
 
-If `codex-rs/` is present (full-tree checkout, e.g. the `feat/fork-cleanup`
-branch), build directly:
+The slim repository has no `codex-rs/` checked in, so build from a
+bootstrapped tree:
 
 ```sh
-cd codex-rs
+bash scripts/bootstrap.sh        # clones openai/codex@BASE_TAG into tree/ and applies patches/
+cd tree
 cargo build --release --bin codex
 ```
 
-To rebuild the tree from scratch from the upstream tag:
+If you happen to have a full-tree checkout (with `codex-rs/` present), build
+directly:
 
 ```sh
-bash scripts/bootstrap.sh        # clones openai/codex@BASE_TAG into tree/ and applies patches/
-cd tree
+cd codex-rs
 cargo build --release --bin codex
 ```
 
@@ -155,9 +207,9 @@ bash scripts/update.sh rust-v0.148.0
 builds and tests, then regenerates `patches/` and updates `BASE_TAG`. If a
 patch conflicts, resolve it in `update-work/` (`git am --continue`), then
 regenerate with `bash scripts/gen-patches.sh rust-v0.148.0`. The patch queue
-never changes version numbers; fork releases are tagged with the plain semver
-(for example `0.148.0`) and the release workflow publishes
-`codex-<target>.tar.gz` + sha256 checksums.
+never changes version numbers; fork releases keep the upstream semver and are
+tagged `rust-v<version>` (for example `rust-v0.148.0`), and the release
+workflow publishes `codex-<target>.tar.gz` + sha256 checksums.
 
 ## License
 
diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index 597448e..e36e8fa 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -87,13 +87,17 @@ use codex_protocol::protocol::AskForApproval;
 use codex_protocol::user_input::UserInput;
 use codex_terminal_detection::TerminalName;
 
-/// Codex CLI
+/// codEx CLI
 ///
 /// If no subcommand is specified, options will be forwarded to the interactive CLI.
 #[derive(Debug, Parser)]
 #[clap(
     author,
-    version,
+    name = "codEx",
+    version = concat!(
+        env!("CARGO_PKG_VERSION"),
+        " (codEx fork, https://github.com/NIyueeE/codEx)"
+    ),
     // If a sub‑command is given, ignore requirements of the default args.
     subcommand_negates_reqs = true,
     // The executable is sometimes invoked via a platform‑specific name like
@@ -3251,6 +3255,23 @@ mod tests {
         assert!(matches!(cli.subcommand, Some(Subcommand::Update)));
     }
 
+    #[test]
+    fn version_string_identifies_codex_fork() {
+        let command = MultitoolCli::command();
+        let version = command.get_version().expect("version");
+        assert_eq!(
+            version,
+            concat!(
+                env!("CARGO_PKG_VERSION"),
+                " (codEx fork, https://github.com/NIyueeE/codEx)"
+            )
+        );
+        assert!(
+            command.get_about().unwrap().to_string().contains("codEx"),
+            "about should mention codEx"
+        );
+    }
+
     #[test]
     fn archive_merges_scoped_tui_flags() {
         let (target, interactive, remote) = finalize_archive_from_args(
diff --git a/codex-rs/cli/tests/sandbox_cloud_config.rs b/codex-rs/cli/tests/sandbox_cloud_config.rs
index 81fbf07..f43276f 100644
--- a/codex-rs/cli/tests/sandbox_cloud_config.rs
+++ b/codex-rs/cli/tests/sandbox_cloud_config.rs
@@ -140,7 +140,9 @@ async fn sandbox_fetches_and_enforces_cloud_managed_permission_profile() -> Resu
     );
     if !nested_macos_sandbox_unavailable {
         assert!(
-            String::from_utf8(output.stdout)?.starts_with("codex"),
+            String::from_utf8(output.stdout)?
+                .to_lowercase()
+                .starts_with("codex"),
             "expected the sandboxed Codex version command to run",
         );
     }
diff --git a/codex-rs/exec/src/event_processor_with_human_output.rs b/codex-rs/exec/src/event_processor_with_human_output.rs
index e3463d7..c6f230c 100644
--- a/codex-rs/exec/src/event_processor_with_human_output.rs
+++ b/codex-rs/exec/src/event_processor_with_human_output.rs
@@ -215,7 +215,7 @@ impl EventProcessor for EventProcessorWithHumanOutput {
         session_configured_event: &SessionConfiguredEvent,
     ) {
         const VERSION: &str = env!("CARGO_PKG_VERSION");
-        eprintln!("OpenAI Codex v{VERSION}\n--------");
+        eprintln!("codEx v{VERSION}\n--------");
         for (key, value) in config_summary_entries(config, session_configured_event) {
             eprintln!("{} {}", format!("{key}:").style(self.bold), value);
         }
diff --git a/codex-rs/login/src/device_code_auth.rs b/codex-rs/login/src/device_code_auth.rs
index a909da4..fc41544 100644
--- a/codex-rs/login/src/device_code_auth.rs
+++ b/codex-rs/login/src/device_code_auth.rs
@@ -149,11 +149,11 @@ async fn poll_for_token(
 fn device_code_prompt(verification_url: &str, code: &str) -> String {
     let version = env!("CARGO_PKG_VERSION");
     format!(
-        "\nWelcome to Codex [v{ANSI_GRAY}{version}{ANSI_RESET}]\n{ANSI_GRAY}OpenAI's command-line coding agent{ANSI_RESET}\n\
+        "\nWelcome to codEx [v{ANSI_GRAY}{version}{ANSI_RESET}]\n{ANSI_GRAY}a community fork of OpenAI's command-line coding agent{ANSI_RESET}\n\
 \nFollow these steps to sign in with ChatGPT using device code authorization:\n\
 \n1. Open this link in your browser and sign in to your account\n   {ANSI_BLUE}{verification_url}{ANSI_RESET}\n\
 \n2. Enter this one-time code {ANSI_GRAY}(expires in 15 minutes){ANSI_RESET}\n   {ANSI_BLUE}{code}{ANSI_RESET}\n\
-\n{ANSI_GRAY}Continue only if you started this login in Codex. If a website or another person gave you this code, cancel.{ANSI_RESET}\n",
+\n{ANSI_GRAY}Continue only if you started this login in codEx. If a website or another person gave you this code, cancel.{ANSI_RESET}\n",
     )
 }
 
diff --git a/codex-rs/login/src/device_code_auth_tests.rs b/codex-rs/login/src/device_code_auth_tests.rs
index ba6071b..5ee6210 100644
--- a/codex-rs/login/src/device_code_auth_tests.rs
+++ b/codex-rs/login/src/device_code_auth_tests.rs
@@ -5,6 +5,6 @@ fn device_code_prompt_renders_phishing_warning() {
     let prompt = device_code_prompt("https://example.com/device", "ABCD-EFGH");
 
     assert!(prompt.contains(
-        "\x1b[90mContinue only if you started this login in Codex. If a website or another person gave you this code, cancel.\x1b[0m"
+        "\x1b[90mContinue only if you started this login in codEx. If a website or another person gave you this code, cancel.\x1b[0m"
     ));
 }
diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index 7c3e07e..1c7fcba 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -158,7 +158,7 @@ impl App {
                 self.chat_widget.maybe_send_next_queued_input();
             }
             AppEvent::OpenRewindMessagePicker { restore_files } => {
-                self.show_rewind_message_picker(restore_files);
+                self.show_rewind_message_picker(tui, restore_files);
                 self.chat_widget.maybe_send_next_queued_input();
             }
             AppEvent::OpenRewindSnapshotPicker => {
@@ -329,9 +329,23 @@ impl App {
                 self.refresh_in_memory_config_from_disk_best_effort("forking the thread")
                     .await;
                 let config = self.fresh_session_config();
-                let turns = match self.thread_event_channels.get(&thread_id) {
-                    Some(channel) => Some(channel.store.lock().await.turns.clone()),
-                    None => None,
+                // The channel's turns are a snapshot taken when the thread was
+                // attached; turns completed since then render live in the
+                // transcript but are never appended to the channel store.
+                // Resolve the selected message against the app server's
+                // canonical persisted turns so messages submitted after attach
+                // (e.g. the newest message) can be rewound, and fall back to
+                // the channel store when a fresh read is unavailable (e.g.
+                // paginated threads without a turns view).
+                let turns = match app_server
+                    .thread_read(thread_id, /*include_turns*/ true)
+                    .await
+                {
+                    Ok(thread) if !thread.turns.is_empty() => Some(thread.turns),
+                    _ => match self.thread_event_channels.get(&thread_id) {
+                        Some(channel) => Some(channel.store.lock().await.turns.clone()),
+                        None => None,
+                    },
                 };
                 let started = match turns {
                     Some(turns) => match crate::app_backtrack::backtrack_fork_before_turn_id(
diff --git a/codex-rs/tui/src/app/tests.rs b/codex-rs/tui/src/app/tests.rs
index 24e36f6..115977d 100644
--- a/codex-rs/tui/src/app/tests.rs
+++ b/codex-rs/tui/src/app/tests.rs
@@ -6060,11 +6060,8 @@ async fn rewind_files_and_conversation_forks_thread_before_selected_message() ->
         /*model_provider*/ None,
         /*git_info*/ None,
     )
-    .map_err(|error| {
-        color_eyre::eyre::eyre!("failed to create fake rollout: {error}")
-    })?;
-    let rollout_path =
-        app_test_support::rollout_path(codex_home.path(), filename_ts, &thread_id);
+    .map_err(|error| color_eyre::eyre::eyre!("failed to create fake rollout: {error}"))?;
+    let rollout_path = app_test_support::rollout_path(codex_home.path(), filename_ts, &thread_id);
     let contents = std::fs::read_to_string(&rollout_path)?;
     // Keep only the session_meta line; rebuild the conversation with explicit
     // turn lifecycle events (task_started / item_completed / task_complete).
@@ -6127,10 +6124,9 @@ async fn rewind_files_and_conversation_forks_thread_before_selected_message() ->
 
     let (mut app, mut app_event_rx, _op_rx) = make_test_app_with_channels().await;
     app.config = config.clone();
-    let mut app_server =
-        crate::start_embedded_app_server_for_picker(&config)
-            .await
-            .expect("embedded app server");
+    let mut app_server = crate::start_embedded_app_server_for_picker(&config)
+        .await
+        .expect("embedded app server");
     let resumed = app_server
         .resume_thread(
             config.clone(),
@@ -6248,6 +6244,330 @@ async fn rewind_files_and_conversation_forks_thread_before_selected_message() ->
     Ok(())
 }
 
+#[tokio::test]
+async fn rewind_message_selection_reuses_transcript_overlay() -> Result<()> {
+    let codex_home = tempfile::tempdir()?;
+    let config = ConfigBuilder::default()
+        .codex_home(codex_home.path().to_path_buf())
+        .loader_overrides(LoaderOverrides::without_managed_config_for_tests())
+        .build()
+        .await?;
+    let (mut app, mut app_event_rx, _op_rx) = make_test_app_with_channels().await;
+    app.config = config.clone();
+    let mut app_server = crate::start_embedded_app_server_for_picker(&config).await?;
+    let mut tui = crate::tui::test_support::make_test_tui()?;
+
+    let thread_id = ThreadId::new();
+    app.chat_widget
+        .handle_thread_session(crate::session_state::ThreadSessionState {
+            thread_id,
+            forked_from_id: None,
+            fork_parent_title: None,
+            thread_name: None,
+            model: "gpt-test".to_string(),
+            model_provider_id: "test-provider".to_string(),
+            service_tier: None,
+            approval_policy: AskForApproval::Never,
+            approvals_reviewer: ApprovalsReviewer::User,
+            permission_profile: PermissionProfile::read_only(),
+            active_permission_profile: None,
+            cwd: test_path_buf("/home/user/project").abs(),
+            runtime_workspace_roots: Vec::new(),
+            instruction_source_paths: Vec::new(),
+            reasoning_effort: None,
+            collaboration_mode: None,
+            personality: None,
+            message_history: None,
+            network_proxy: None,
+            rollout_path: Some(PathBuf::new()),
+        });
+    let user_cell = |text: &str| -> Arc<dyn HistoryCell> {
+        Arc::new(UserHistoryCell {
+            message: text.to_string(),
+            text_elements: Vec::new(),
+            local_image_paths: Vec::new(),
+            remote_image_urls: Vec::new(),
+        }) as Arc<dyn HistoryCell>
+    };
+    app.transcript_cells = vec![
+        user_cell("first question"),
+        plain_line_cell("answer first"),
+        user_cell("second question"),
+        plain_line_cell("answer second"),
+    ];
+
+    // "Conversation only": the transcript overlay opens with the newest user
+    // message highlighted, reusing the old double-Esc backtrack picker.
+    app.show_rewind_message_picker(&mut tui, /*restore_files*/ false);
+    assert!(matches!(&app.overlay, Some(Overlay::Transcript(_))));
+    assert!(app.backtrack.overlay_preview_active);
+    assert_eq!(app.backtrack.nth_user_message, 1);
+    assert_eq!(
+        app.backtrack.confirm_mode,
+        crate::app_backtrack::BacktrackConfirmMode::Rewind {
+            restore_files: false
+        }
+    );
+
+    // Esc steps to the older message; Enter confirms the rewind fork without
+    // touching files.
+    app.handle_backtrack_overlay_event(
+        &mut tui,
+        &mut app_server,
+        TuiEvent::Key(KeyEvent::new(KeyCode::Esc, KeyModifiers::NONE)),
+    )
+    .await?;
+    assert_eq!(app.backtrack.nth_user_message, 0);
+    assert!(
+        matches!(&app.overlay, Some(Overlay::Transcript(_))),
+        "overlay stays open while stepping"
+    );
+    app.handle_backtrack_overlay_event(
+        &mut tui,
+        &mut app_server,
+        TuiEvent::Key(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE)),
+    )
+    .await?;
+    assert!(app.overlay.is_none(), "overlay closes on confirm");
+    assert_eq!(
+        app.backtrack.confirm_mode,
+        crate::app_backtrack::BacktrackConfirmMode::EditPrompt,
+        "state resets after confirm"
+    );
+    let events: Vec<AppEvent> = std::iter::from_fn(|| app_event_rx.try_recv().ok()).collect();
+    assert!(
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::ForkSessionForPromptEdit {
+                thread_id: event_thread_id,
+                nth_user_message: 0,
+                prompt,
+            } if *event_thread_id == thread_id && prompt.text == "first question"
+        )),
+        "should fork before the selected message: {events:?}"
+    );
+    assert!(
+        !events
+            .iter()
+            .any(|event| matches!(event, AppEvent::RewindRestoreFiles { .. })),
+        "conversation-only rewind must not restore files"
+    );
+
+    // "Files and conversation": confirming the newest message also restores
+    // the snapshot taken for that turn.
+    app.show_rewind_message_picker(&mut tui, /*restore_files*/ true);
+    assert_eq!(
+        app.backtrack.confirm_mode,
+        crate::app_backtrack::BacktrackConfirmMode::Rewind {
+            restore_files: true
+        }
+    );
+    assert_eq!(app.backtrack.nth_user_message, 1);
+    app.handle_backtrack_overlay_event(
+        &mut tui,
+        &mut app_server,
+        TuiEvent::Key(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE)),
+    )
+    .await?;
+    let events: Vec<AppEvent> = std::iter::from_fn(|| app_event_rx.try_recv().ok()).collect();
+    assert!(
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::ForkSessionForPromptEdit {
+                thread_id: event_thread_id,
+                nth_user_message: 1,
+                prompt,
+            } if *event_thread_id == thread_id && prompt.text == "second question"
+        )),
+        "should fork before the newest message: {events:?}"
+    );
+    assert!(
+        events.iter().any(|event| matches!(
+            event,
+            AppEvent::RewindRestoreFiles { index: 2, prompt }
+                if prompt == "second question"
+        )),
+        "should restore the snapshot for the selected turn: {events:?}"
+    );
+
+    app_server.shutdown().await?;
+    Ok(())
+}
+
+#[tokio::test]
+async fn rewind_forks_newest_message_completed_after_attach() -> Result<()> {
+    // A turn completed after the TUI attached renders in the transcript but is
+    // never appended to the thread channel's turns snapshot. /rewind must still
+    // resolve the newest message against the app server's canonical turns
+    // instead of failing with "the selected prompt was not found in the
+    // persisted thread".
+    let codex_home = tempfile::tempdir()?;
+    let workspace = tempfile::tempdir()?;
+    let filename_ts = "2025-01-05T12-00-00";
+    let meta_rfc3339 = "2025-01-05T12:00:00Z";
+    let thread_id = app_test_support::create_fake_rollout(
+        codex_home.path(),
+        filename_ts,
+        meta_rfc3339,
+        "first question",
+        /*model_provider*/ None,
+        /*git_info*/ None,
+    )
+    .map_err(|error| color_eyre::eyre::eyre!("failed to create fake rollout: {error}"))?;
+    let rollout_path = app_test_support::rollout_path(codex_home.path(), filename_ts, &thread_id);
+    let contents = std::fs::read_to_string(&rollout_path)?;
+    // Keep only the session_meta line; rebuild the conversation with explicit
+    // turn lifecycle events (task_started / item_completed / task_complete).
+    let session_meta_line = contents
+        .lines()
+        .next()
+        .expect("session meta line")
+        .to_string();
+    let mut lines = vec![session_meta_line];
+    let event_line = |payload: serde_json::Value| {
+        serde_json::json!({
+            "timestamp": meta_rfc3339,
+            "type": "event_msg",
+            "payload": payload,
+        })
+        .to_string()
+    };
+    for (turn_id, prompt) in [
+        ("turn-1", "first question"),
+        ("turn-2", "second question"),
+        ("turn-3", "third question"),
+    ] {
+        lines.push(event_line(serde_json::json!({
+            "type": "task_started",
+            "turn_id": turn_id,
+            "model_context_window": null,
+        })));
+        lines.push(event_line(serde_json::json!({
+            "type": "user_message",
+            "message": prompt,
+            "kind": "plain",
+        })));
+        lines.push(event_line(serde_json::json!({
+            "type": "agent_message",
+            "message": "ok",
+        })));
+        lines.push(event_line(serde_json::json!({
+            "type": "task_complete",
+            "turn_id": turn_id,
+            "last_agent_message": null,
+            "error": null,
+            "started_at": 10,
+            "completed_at": 20,
+            "duration_ms": 10_000,
+            "time_to_first_token_ms": null,
+        })));
+    }
+    std::fs::write(&rollout_path, lines.join("\n") + "\n")?;
+
+    let config = ConfigBuilder::default()
+        .codex_home(codex_home.path().to_path_buf())
+        .loader_overrides(LoaderOverrides::without_managed_config_for_tests())
+        .harness_overrides(ConfigOverrides {
+            cwd: Some(workspace.path().to_path_buf()),
+            ..Default::default()
+        })
+        .build()
+        .await?;
+    assert!(config.rewind_enabled);
+
+    let (mut app, mut app_event_rx, _op_rx) = make_test_app_with_channels().await;
+    app.config = config.clone();
+    let mut app_server = crate::start_embedded_app_server_for_picker(&config)
+        .await
+        .expect("embedded app server");
+    let resumed = app_server
+        .resume_thread(
+            config.clone(),
+            ThreadId::from_string(thread_id.as_str())?,
+            crate::app_server_session::ResumeModelSettings::RestoreFromThread,
+        )
+        .await
+        .expect("resume rollout thread");
+    let thread_id = resumed.session.thread_id;
+    let mut tui = crate::tui::test_support::make_test_tui()?;
+    app.enqueue_primary_thread_session(resumed.session, resumed.turns)
+        .await
+        .expect("primary thread should be registered");
+    // History replay is delivered as app events; run them through the event
+    // loop so `transcript_cells` is populated like in a real session.
+    while let Ok(event) = app_event_rx.try_recv() {
+        app.handle_event(&mut tui, &mut app_server, event).await?;
+    }
+
+    // Simulate a turn that completed after attach: the transcript knows all
+    // three user messages, but the channel's turns snapshot only has the
+    // first two (turns completed later are rendered live, never appended).
+    {
+        let mut store = app
+            .thread_event_channels
+            .get(&thread_id)
+            .expect("thread channel")
+            .store
+            .lock()
+            .await;
+        assert_eq!(store.turns.len(), 3);
+        store.turns.truncate(2);
+    }
+
+    // Exactly what the "Conversation only" picker action sends for the newest
+    // message ("third question", transcript ordinal 2).
+    app.handle_event(
+        &mut tui,
+        &mut app_server,
+        AppEvent::ForkSessionForPromptEdit {
+            thread_id,
+            nth_user_message: 2,
+            prompt: crate::chatwidget::UserMessage::from("third question"),
+        },
+    )
+    .await?;
+    // The fork handler queues the forked thread's replay as app events; run
+    // them through the event loop like the real main loop would.
+    while let Ok(event) = app_event_rx.try_recv() {
+        app.handle_event(&mut tui, &mut app_server, event).await?;
+    }
+
+    let forked_thread_id = app
+        .chat_widget
+        .thread_id()
+        .expect("forked thread should be attached");
+    assert_ne!(forked_thread_id, thread_id);
+    let transcript = app
+        .transcript_cells
+        .iter()
+        .map(|cell| lines_to_single_string(&cell.display_lines(/*width*/ 120)))
+        .collect::<Vec<_>>()
+        .join("\n");
+    assert!(
+        !transcript.contains("Failed to branch"),
+        "fork should succeed even when the newest turn is missing from the channel snapshot:\n{transcript}"
+    );
+    assert!(
+        transcript.contains("first question"),
+        "forked thread should keep the first turn:\n{transcript}"
+    );
+    assert!(
+        transcript.contains("second question"),
+        "forked thread should keep the second turn:\n{transcript}"
+    );
+    assert!(
+        !transcript.contains("third question"),
+        "forked thread should drop the selected turn:\n{transcript}"
+    );
+    assert_eq!(
+        app.chat_widget.composer_text_with_pending(),
+        "third question",
+        "selected prompt should be restored to the composer"
+    );
+    app_server.shutdown().await?;
+    Ok(())
+}
+
 #[tokio::test]
 async fn remote_resume_current_cwd_rejection_snapshot() -> Result<()> {
     let (mut app, mut app_event_rx, _op_rx) = make_test_app_with_channels().await;
diff --git a/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs b/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs
index c560f72..9d86935 100644
--- a/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs
+++ b/codex-rs/tui/src/app/tests/session_lifecycle_requests.rs
@@ -933,14 +933,14 @@ async fn underfilled_scrollback_fetches_older_pages_without_opening_the_transcri
         };
         let partial = render_overlay(overlay);
         assert!(partial.contains("Earlier messages are available — scroll up to load them"));
-        assert!(!partial.contains("OpenAI Codex"));
+        assert!(!partial.contains("codEx"));
         assert!(!partial.contains("This is a test announcement"));
         assert!(!partial.contains('%'));
 
         overlay.set_history_state(crate::pager_overlay::TranscriptHistoryState::LoadingOlder);
         let loading = render_overlay(overlay);
         assert!(loading.contains("Loading earlier messages..."));
-        assert!(!loading.contains("OpenAI Codex"));
+        assert!(!loading.contains("codEx"));
         assert!(!loading.contains('%'));
     } else {
         panic!("expected transcript overlay");
diff --git a/codex-rs/tui/src/app_backtrack.rs b/codex-rs/tui/src/app_backtrack.rs
index 2c4a933..e30fd35 100644
--- a/codex-rs/tui/src/app_backtrack.rs
+++ b/codex-rs/tui/src/app_backtrack.rs
@@ -47,7 +47,17 @@ const NO_PREVIOUS_MESSAGE_TO_EDIT: &str = "No previous message to edit.";
 pub(crate) const SIDE_EDIT_PREVIOUS_UNAVAILABLE_MESSAGE: &str =
     "Editing previous prompts is unavailable in side conversations.";
 
-/// Aggregates all backtrack-related state used by the App.
+/// What confirming a highlighted transcript selection should do.
+#[derive(Debug, Default, Clone, Copy, PartialEq, Eq)]
+pub(crate) enum BacktrackConfirmMode {
+    /// Original backtrack: fork before the prompt and restore it for editing.
+    #[default]
+    EditPrompt,
+    /// `/rewind`: fork before the prompt, optionally restoring workspace files.
+    Rewind { restore_files: bool },
+}
+
+/// Aggregates all backtrack/rewind transcript-selection state used by the App.
 #[derive(Default)]
 pub(crate) struct BacktrackState {
     /// True when Esc has primed backtrack mode in the main view.
@@ -63,6 +73,8 @@ pub(crate) struct BacktrackState {
     pub(crate) nth_user_message: usize,
     /// True when the transcript overlay is showing a backtrack preview.
     pub(crate) overlay_preview_active: bool,
+    /// How a confirmed selection is applied.
+    pub(crate) confirm_mode: BacktrackConfirmMode,
 }
 
 /// A user-visible backtrack choice that can be reopened on a source-preserving branch.
@@ -75,6 +87,31 @@ pub(crate) struct BacktrackSelection {
 }
 
 impl App {
+    /// Opens the transcript overlay for `/rewind` message selection.
+    ///
+    /// Reuses the original double-Esc backtrack picker: the full conversation
+    /// history is shown with the newest user message highlighted, Esc/Left
+    /// step to older messages, Right steps newer, and Enter confirms. Returns
+    /// `false` when there is nothing to rewind.
+    pub(crate) fn begin_rewind_overlay_selection(
+        &mut self,
+        tui: &mut tui::Tui,
+        restore_files: bool,
+    ) -> bool {
+        if !has_backtrack_target(&self.transcript_cells) {
+            return false;
+        }
+        self.backtrack.confirm_mode = BacktrackConfirmMode::Rewind { restore_files };
+        self.backtrack.base_id = self.chat_widget.thread_id();
+        self.backtrack.overlay_preview_active = true;
+        self.open_transcript_overlay(tui);
+        if let Some(last) = user_count(&self.transcript_cells).checked_sub(1) {
+            self.apply_backtrack_selection_internal(last);
+        }
+        tui.frame_requester().schedule_frame();
+        true
+    }
+
     /// Route overlay events while the transcript overlay is active.
     ///
     /// If backtrack preview is active, Esc / Left steps selection, Right steps forward, Enter
@@ -359,15 +396,43 @@ impl App {
         Ok(())
     }
 
-    /// Handle Enter in overlay backtrack preview: confirm selection and reset state.
+    /// Handle Enter in overlay preview: confirm selection and reset state.
+    ///
+    /// In backtrack mode this forks before the selected prompt and restores it
+    /// for editing; in `/rewind` mode it forks before the prompt and optionally
+    /// restores workspace files (mirroring the old `/rewind` message picker
+    /// actions).
     fn overlay_confirm_backtrack(&mut self, tui: &mut tui::Tui) {
         let nth_user_message = self.backtrack.nth_user_message;
         let selection = self.backtrack_selection(nth_user_message);
+        let mode = self.backtrack.confirm_mode;
         self.close_transcript_overlay(tui);
-        if let Some(selection) = selection {
-            self.apply_backtrack_selection(selection);
+        let Some(selection) = selection else {
             tui.frame_requester().schedule_frame();
+            return;
+        };
+        match mode {
+            BacktrackConfirmMode::EditPrompt => self.apply_backtrack_selection(selection),
+            BacktrackConfirmMode::Rewind { restore_files } => {
+                // Snapshots are numbered from 1 (one per submitted user
+                // message, taken before its turn), so the snapshot for message
+                // `nth` (0-based transcript ordinal) is `nth + 1`.
+                let index = nth_user_message as u64 + 1;
+                let prompt_text = selection.prompt.text.clone();
+                self.app_event_tx.send(AppEvent::ForkSessionForPromptEdit {
+                    thread_id: selection.thread_id,
+                    nth_user_message: selection.nth_user_message,
+                    prompt: selection.prompt,
+                });
+                if restore_files {
+                    self.app_event_tx.send(AppEvent::RewindRestoreFiles {
+                        index,
+                        prompt: prompt_text,
+                    });
+                }
+            }
         }
+        tui.frame_requester().schedule_frame();
     }
 
     /// Handle Esc in overlay backtrack preview: step selection if armed, else forward.
@@ -399,6 +464,7 @@ impl App {
         self.backtrack.primed = false;
         self.backtrack.base_id = None;
         self.backtrack.nth_user_message = usize::MAX;
+        self.backtrack.confirm_mode = BacktrackConfirmMode::EditPrompt;
     }
 
     fn backtrack_selection(&self, nth_user_message: usize) -> Option<BacktrackSelection> {
@@ -442,7 +508,9 @@ impl App {
     }
 
     /// Lists rewindable user messages with their transcript ordinals, most
-    /// recent first. Used by the `/rewind` conversation picker.
+    /// recent first. Tests use this to mirror what the `/rewind` transcript
+    /// picker offers (the picker itself highlights cells directly).
+    #[cfg(test)]
     pub(crate) fn rewind_message_selections(&self) -> Vec<(usize, BacktrackSelection)> {
         let count = user_count(&self.transcript_cells);
         (0..count)
diff --git a/codex-rs/tui/src/history_cell/session.rs b/codex-rs/tui/src/history_cell/session.rs
index 03df014..05c9723 100644
--- a/codex-rs/tui/src/history_cell/session.rs
+++ b/codex-rs/tui/src/history_cell/session.rs
@@ -317,10 +317,10 @@ impl HistoryCell for SessionHeaderHistoryCell {
 
         let make_row = |spans: Vec<Span<'static>>| Line::from(spans);
 
-        // Title line rendered inside the box: ">_ OpenAI Codex (vX)"
+        // Title line rendered inside the box: ">_ codEx (vX)"
         let title_spans: Vec<Span<'static>> = vec![
             Span::from(">_ ").dim(),
-            Span::from("OpenAI Codex").bold(),
+            Span::from("codEx").bold(),
             Span::from(" ").dim(),
             Span::from(format!("(v{})", self.version)).dim(),
         ];
@@ -391,7 +391,7 @@ impl HistoryCell for SessionHeaderHistoryCell {
 
     fn raw_lines(&self) -> Vec<Line<'static>> {
         let mut lines = vec![
-            Line::from(format!("OpenAI Codex (v{})", self.version)),
+            Line::from(format!("codEx (v{})", self.version)),
             Line::from(format!(
                 "model: {}{}",
                 self.model,
diff --git a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_clamps_to_narrow_width.snap b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_clamps_to_narrow_width.snap
index 2baf486..f792c3f 100644
--- a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_clamps_to_narrow_width.snap
+++ b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_clamps_to_narrow_width.snap
@@ -3,7 +3,7 @@ source: tui/src/history_cell/tests.rs
 expression: "render_lines(&lines).join(\"\\n\")"
 ---
 ╭──────────────────────────────────────────╮
-│ >_ OpenAI Codex (vtest)                  │
+│ >_ codEx (vtest)                         │
 │                                          │
 │ model:       gpt-5.6-sol xhigh   fast  … │
 │ directory:   project                     │
diff --git a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_directory.snap b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_directory.snap
index 51d3652..f23bfd0 100644
--- a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_directory.snap
+++ b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_directory.snap
@@ -6,7 +6,7 @@ Buffer {
     area: Rect { x: 0, y: 0, width: 42, height: 6 },
     content: [
         "╭────────────────────────────────────────╮",
-        "│ >_ OpenAI Codex (vtest)                │",
+        "│ >_ codEx (vtest)                       │",
         "│                                        │",
         "│ model:     gpt-5   /model to change    │",
         "│ directory: …ﾊﾟｶﾞﾊﾟｶﾞﾊﾟｶﾞﾊﾟｶﾞﾊﾟ-project │", // hidden by multi-width symbols: [(15, " "), (17, " "), (19, " "), (21, " "), (23, " "), (25, " "), (27, " "), (29, " "), (31, " ")]
@@ -15,7 +15,7 @@ Buffer {
     styles: [
         x: 0, y: 0, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
         x: 5, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: BOLD,
-        x: 17, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
+        x: 10, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
         x: 13, y: 3, fg: Reset, bg: Reset, underline: Reset, modifier: NONE,
         x: 18, y: 3, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
         x: 21, y: 3, fg: Cyan, bg: Reset, underline: Reset, modifier: NONE,
diff --git a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_sound_marks.snap b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_sound_marks.snap
index 0fb1727..1b328bb 100644
--- a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_sound_marks.snap
+++ b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_halfwidth_sound_marks.snap
@@ -6,7 +6,7 @@ Buffer {
     area: Rect { x: 0, y: 0, width: 80, height: 6 },
     content: [
         "╭───────────────────────────────────────────╮                                   ",
-        "│ >_ OpenAI Codex (vtest)                   │                                   ",
+        "│ >_ codEx (vtest)                          │                                   ",
         "│                                           │                                   ",
         "│ model:     gpt-5-ｶﾞ-ﾊﾟ   /model to change │                                   ", // hidden by multi-width symbols: [(20, " "), (23, " ")]
         "│ directory: project                        │                                   ",
@@ -17,7 +17,7 @@ Buffer {
         x: 45, y: 0, fg: Reset, bg: Reset, underline: Reset, modifier: NONE,
         x: 0, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
         x: 5, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: BOLD,
-        x: 17, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
+        x: 10, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
         x: 45, y: 1, fg: Reset, bg: Reset, underline: Reset, modifier: NONE,
         x: 0, y: 2, fg: Reset, bg: Reset, underline: Reset, modifier: DIM,
         x: 45, y: 2, fg: Reset, bg: Reset, underline: Reset, modifier: NONE,
diff --git a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_indicates_yolo_mode.snap b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_indicates_yolo_mode.snap
index 04f6458..3669f3c 100644
--- a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_indicates_yolo_mode.snap
+++ b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_header_indicates_yolo_mode.snap
@@ -1,9 +1,9 @@
 ---
-source: tui/src/history_cell.rs
+source: tui/src/history_cell/tests.rs
 expression: rendered
 ---
 ╭───────────────────────────────────────╮
-│ >_ OpenAI Codex (vtest)               │
+│ >_ codEx (vtest)                      │
 │                                       │
 │ model:       gpt-5   /model to change │
 │ directory:   /tmp/project             │
diff --git a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_info_availability_nux_tooltip_snapshot.snap b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_info_availability_nux_tooltip_snapshot.snap
index 834b366..fa543cb 100644
--- a/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_info_availability_nux_tooltip_snapshot.snap
+++ b/codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__session_info_availability_nux_tooltip_snapshot.snap
@@ -3,7 +3,7 @@ source: tui/src/history_cell/tests.rs
 expression: rendered
 ---
 ╭─────────────────────────────────────╮
-│ >_ OpenAI Codex (v0.147.0)          │
+│ >_ codEx (v0.147.0)                 │
 │                                     │
 │ model:     gpt-5   /model to change │
 │ directory: /tmp/project             │
diff --git a/codex-rs/tui/src/onboarding/welcome.rs b/codex-rs/tui/src/onboarding/welcome.rs
index 880e587..4303521 100644
--- a/codex-rs/tui/src/onboarding/welcome.rs
+++ b/codex-rs/tui/src/onboarding/welcome.rs
@@ -94,8 +94,8 @@ impl WidgetRef for &WelcomeWidget {
         lines.push(Line::from(vec![
             "  ".into(),
             "Welcome to ".into(),
-            "Codex".bold(),
-            ", OpenAI's command-line coding agent".into(),
+            "codEx".bold(),
+            ", a community fork of OpenAI's command-line coding agent".into(),
         ]));
 
         Paragraph::new(lines)
diff --git a/codex-rs/tui/src/rewind/actions.rs b/codex-rs/tui/src/rewind/actions.rs
index 56b65da..995e5e9 100644
--- a/codex-rs/tui/src/rewind/actions.rs
+++ b/codex-rs/tui/src/rewind/actions.rs
@@ -2,12 +2,12 @@
 //! file restoration. The file snapshot engine lives in [`super`].
 
 use crate::app::App;
-use crate::app_backtrack::BacktrackSelection;
 use crate::app_event::AppEvent;
 use crate::bottom_pane::SelectionItem;
 use crate::bottom_pane::SelectionViewParams;
 use crate::bottom_pane::popup_consts::standard_popup_hint_line;
 use crate::rewind;
+use crate::tui;
 
 const REWIND_TITLE: &str = "Rewind";
 const NO_MESSAGES_TO_REWIND: &str = "No previous messages to rewind.";
@@ -21,63 +21,13 @@ impl App {
             .show_selection_view(rewind_scope_selection_params());
     }
 
-    /// Shows the searchable message picker for conversation rollback.
-    pub(crate) fn show_rewind_message_picker(&mut self, restore_files: bool) {
-        let selections = self.rewind_message_selections();
-        if selections.is_empty() {
+    /// Opens the transcript overlay for conversation rollback, reusing the
+    /// original double-Esc backtrack picker instead of a custom list.
+    pub(crate) fn show_rewind_message_picker(&mut self, tui: &mut tui::Tui, restore_files: bool) {
+        if !self.begin_rewind_overlay_selection(tui, restore_files) {
             self.chat_widget
                 .add_info_message(NO_MESSAGES_TO_REWIND.to_string(), /*hint*/ None);
-            return;
         }
-
-        let items = selections
-            .into_iter()
-            .map(|(nth, selection)| {
-                // Snapshots are numbered from 1 (one per submitted user
-                // message, taken before its turn), so the snapshot for message
-                // `nth` (0-based transcript ordinal) is `nth + 1`.
-                let index = nth as u64 + 1;
-                let prompt_text = selection.prompt.text.clone();
-                let thread_id = selection.thread_id;
-                let prompt = selection.prompt.clone();
-                SelectionItem {
-                    name: message_one_line(&selection),
-                    description: Some(format!("Message {}", nth + 1)),
-                    search_value: Some(selection.prompt.text),
-                    actions: vec![Box::new(move |tx| {
-                        tx.send(AppEvent::ForkSessionForPromptEdit {
-                            thread_id,
-                            nth_user_message: nth,
-                            prompt: prompt.clone(),
-                        });
-                        if restore_files {
-                            tx.send(AppEvent::RewindRestoreFiles {
-                                index,
-                                prompt: prompt_text.clone(),
-                            });
-                        }
-                    })],
-                    dismiss_on_select: true,
-                    ..Default::default()
-                }
-            })
-            .collect();
-
-        let subtitle = if restore_files {
-            "Pick a message to rewind to; workspace files are restored to that point"
-        } else {
-            "Pick a message to rewind the conversation to"
-        };
-        self.chat_widget.show_selection_view(SelectionViewParams {
-            view_id: Some("rewind_messages"),
-            title: Some(REWIND_TITLE.to_string()),
-            subtitle: Some(subtitle.to_string()),
-            footer_hint: Some(standard_popup_hint_line()),
-            is_searchable: true,
-            search_placeholder: Some("Search messages".to_string()),
-            items,
-            ..Default::default()
-        });
     }
 
     /// Shows the snapshot picker for file-only rollback.
@@ -214,24 +164,6 @@ pub(crate) fn rewind_scope_selection_params() -> SelectionViewParams {
     }
 }
 
-fn message_one_line(selection: &BacktrackSelection) -> String {
-    let first_line = selection
-        .prompt
-        .text
-        .lines()
-        .next()
-        .unwrap_or_default()
-        .trim();
-    if first_line.is_empty() {
-        return "(attachment)".to_string();
-    }
-    let mut name = first_line.chars().take(90).collect::<String>();
-    if first_line.chars().count() > 90 {
-        name.push('…');
-    }
-    name
-}
-
 fn snapshot_label(snapshot: &rewind::RewindSnapshotInfo) -> String {
     let timestamp = chrono::DateTime::from_timestamp(snapshot.created_at, /*nsecs*/ 0)
         .map(|dt| dt.format("%Y-%m-%d %H:%M").to_string())
diff --git a/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_after_long_transcript_fresh_header_only.snap b/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_after_long_transcript_fresh_header_only.snap
index 98d6015..5ca1295 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_after_long_transcript_fresh_header_only.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_after_long_transcript_fresh_header_only.snap
@@ -1,9 +1,9 @@
 ---
-source: tui/src/app.rs
+source: tui/src/app/tests.rs
 expression: rendered
 ---
 ╭─────────────────────────────────────────────╮
-│ >_ OpenAI Codex (v<VERSION>)                │
+│ >_ codEx (v<VERSION>)                       │
 │                                             │
 │ model:     gpt-test high   /model to change │
 │ directory: /tmp/project                     │
diff --git a/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_header_fast_status_fast_capable_models.snap b/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_header_fast_status_fast_capable_models.snap
index 08afd02..d545154 100644
--- a/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_header_fast_status_fast_capable_models.snap
+++ b/codex-rs/tui/src/snapshots/codex_tui__app__tests__clear_ui_header_fast_status_fast_capable_models.snap
@@ -1,9 +1,9 @@
 ---
-source: tui/src/app.rs
+source: tui/src/app/tests.rs
 expression: rendered
 ---
 ╭────────────────────────────────────────────────────╮
-│ >_ OpenAI Codex (v<VERSION>)                       │
+│ >_ codEx (v<VERSION>)                              │
 │                                                    │
 │ model:     gpt-5.4 xhigh   fast   /model to change │
 │ directory: /tmp/project                            │
diff --git a/codex-rs/tui/src/status/card.rs b/codex-rs/tui/src/status/card.rs
index 382a9e3..7a36643 100644
--- a/codex-rs/tui/src/status/card.rs
+++ b/codex-rs/tui/src/status/card.rs
@@ -708,7 +708,7 @@ impl HistoryCell for StatusHistoryCell {
         let mut lines: Vec<Line<'static>> = Vec::new();
         lines.push(Line::from(vec![
             Span::from(format!("{}>_ ", FieldFormatter::INDENT)).dim(),
-            Span::from("OpenAI Codex").bold(),
+            Span::from("codEx").bold(),
             Span::from(" ").dim(),
             Span::from(format!("(v{CODEX_CLI_VERSION})")).dim(),
         ]));
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_cached_limits_hide_credits_without_flag.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_cached_limits_hide_credits_without_flag.snap
index e31d5ea..f59469b 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_cached_limits_hide_credits_without_flag.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_cached_limits_hide_credits_without_flag.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_credits_and_limits.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_credits_and_limits.snap
index e979851..c1f778c 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_credits_and_limits.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_credits_and_limits.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_enterprise_monthly_credit_limit.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_enterprise_monthly_credit_limit.snap
index 8cd4230..d93c4be 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_enterprise_monthly_credit_limit.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_enterprise_monthly_credit_limit.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭───────────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                       │
+│  >_ codEx (v0.147.0)                                                              │
 │                                                                                   │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date                     │
 │ information on rate limits and credits                                            │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_forked_from.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_forked_from.snap
index b8f0fa4..9e4cf83 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_forked_from.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_forked_from.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_monthly_limit.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_monthly_limit.snap
index 3efc809..e4fbccd 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_monthly_limit.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_monthly_limit.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_reasoning_details.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_reasoning_details.snap
index 8ba0ae6..a3434ac 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_reasoning_details.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_includes_reasoning_details.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭───────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                               │
+│  >_ codEx (v0.147.0)                                                      │
 │                                                                           │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date             │
 │ information on rate limits and credits                                    │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_active_user_defined_profile.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_active_user_defined_profile.snap
index 924f079..1bba72d 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_active_user_defined_profile.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_active_user_defined_profile.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭───────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                           │
+│  >_ codEx (v0.147.0)                                                  │
 │                                                                       │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date         │
 │ information on rate limits and credits                                │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_auto_review_permissions.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_auto_review_permissions.snap
index e70f108..eb445ae 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_auto_review_permissions.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_auto_review_permissions.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭───────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                           │
+│  >_ codEx (v0.147.0)                                                  │
 │                                                                       │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date         │
 │ information on rate limits and credits                                │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_chatgpt_plan_without_email.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_chatgpt_plan_without_email.snap
index 10957de..b4d39d0 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_chatgpt_plan_without_email.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_chatgpt_plan_without_email.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭──────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                              │
+│  >_ codEx (v0.147.0)                                                     │
 │                                                                          │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date            │
 │ information on rate limits and credits                                   │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_missing_limits_message.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_missing_limits_message.snap
index 1c4b322..0503736 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_missing_limits_message.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_missing_limits_message.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_refreshing_limits_notice.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_refreshing_limits_notice.snap
index c368ac5..acafc39 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_refreshing_limits_notice.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_refreshing_limits_notice.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_stale_limits_message.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_stale_limits_message.snap
index cf78a16..57444c8 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_stale_limits_message.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_stale_limits_message.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_unavailable_limits_message.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_unavailable_limits_message.snap
index 01e303f..9f75390 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_unavailable_limits_message.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_shows_unavailable_limits_message.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_treats_refreshing_empty_limits_as_unavailable.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_treats_refreshing_empty_limits_as_unavailable.snap
index 01e303f..9f75390 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_treats_refreshing_empty_limits_as_unavailable.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_treats_refreshing_empty_limits_as_unavailable.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_halfwidth_kana_in_narrow_terminal.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_halfwidth_kana_in_narrow_terminal.snap
index 3e0f83a..0a275be 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_halfwidth_kana_in_narrow_terminal.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_halfwidth_kana_in_narrow_terminal.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)            │
+│  >_ codEx (v0.147.0)                   │
 │                                        │
 │ Visit                                  │
 │ https://chatgpt.com/codex/settings/usa │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_in_narrow_terminal.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_in_narrow_terminal.snap
index bf8ea8f..37b0a0c 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_in_narrow_terminal.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_truncates_in_narrow_terminal.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                        │
+│  >_ codEx (v0.147.0)                                               │
 │                                                                    │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date      │
 │ information on rate limits and credits                             │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_default_reasoning_when_config_empty.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_default_reasoning_when_config_empty.snap
index ca09c25..a71e350 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_default_reasoning_when_config_empty.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_default_reasoning_when_config_empty.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭─────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                 │
+│  >_ codEx (v0.147.0)                                                        │
 │                                                                             │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
 │ information on rate limits and credits                                      │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_generic_limit_labels_for_unsupported_windows.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_generic_limit_labels_for_unsupported_windows.snap
index fec2013..7de010f 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_generic_limit_labels_for_unsupported_windows.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_uses_generic_limit_labels_for_unsupported_windows.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭──────────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                                  │
+│  >_ codEx (v0.147.0)                                                         │
 │                                                                              │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date                │
 │ information on rate limits and credits                                       │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_wraps_enterprise_monthly_credit_details_in_narrow_terminal.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_wraps_enterprise_monthly_credit_details_in_narrow_terminal.snap
index 7934254..9d3022f 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_wraps_enterprise_monthly_credit_details_in_narrow_terminal.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__status_snapshot_wraps_enterprise_monthly_credit_details_in_narrow_terminal.snap
@@ -5,7 +5,7 @@ expression: sanitized
 /status
 
 ╭────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                │
+│  >_ codEx (v0.147.0)                       │
 │                                            │
 │ Visit                                      │
 │ https://chatgpt.com/codex/settings/usage   │
diff --git a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__transcript_overlay_status_rate_limit_refresh.snap b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__transcript_overlay_status_rate_limit_refresh.snap
index d65b84b..d61183c 100644
--- a/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__transcript_overlay_status_rate_limit_refresh.snap
+++ b/codex-rs/tui/src/status/snapshots/codex_tui__status__tests__transcript_overlay_status_rate_limit_refresh.snap
@@ -7,7 +7,7 @@ before:
 /status
 
 ╭──────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                              │
+│  >_ codEx (v0.147.0)                                                     │
 │                                                                          │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date            │
 │ information on rate limits and credits                                   │
@@ -39,7 +39,7 @@ after:
 /status
 
 ╭───────────────────────────────────────────────────────────────────────────╮
-│  >_ OpenAI Codex (v0.147.0)                                               │
+│  >_ codEx (v0.147.0)                                                      │
 │                                                                           │
 │ Visit https://chatgpt.com/codex/settings/usage for up-to-date             │
 │ information on rate limits and credits                                    │
diff --git a/codex-rs/tui/src/tooltips.rs b/codex-rs/tui/src/tooltips.rs
index 26e090d..0ba6e96 100644
--- a/codex-rs/tui/src/tooltips.rs
+++ b/codex-rs/tui/src/tooltips.rs
@@ -10,7 +10,7 @@ const APP_TOOLTIP: &str = "Try the **Desktop app**. Run 'codex app' or visit htt
 const FAST_TOOLTIP: &str =
     "*New* Use **/fast** to enable our fastest inference with increased plan usage.";
 const OTHER_TOOLTIP: &str = "*New* Build faster with the **Desktop app**. Run 'codex app' or visit https://chatgpt.com/codex?app-landing-page=true";
-const OTHER_TOOLTIP_NON_MAC: &str = "*New* Build faster with Codex.";
+const OTHER_TOOLTIP_NON_MAC: &str = "*New* Build faster with codEx.";
 const FREE_GO_TOOLTIP: &str =
     "*New* For a limited time, Codex is included in your plan for free – let’s build together.";
 
diff --git a/codex-rs/tui/tests/suite/focus_palette.rs b/codex-rs/tui/tests/suite/focus_palette.rs
index c820024..c8d63d2 100644
--- a/codex-rs/tui/tests/suite/focus_palette.rs
+++ b/codex-rs/tui/tests/suite/focus_palette.rs
@@ -125,7 +125,7 @@ impl PtyCodex {
             self.read_output(Duration::from_millis(/*millis*/ 50))?;
             self.answer_startup_queries()?;
 
-            if self.palette_answered && self.screen_contains("OpenAI Codex") {
+            if self.palette_answered && self.screen_contains("codEx") {
                 return Ok(());
             }
 
-- 
2.55.0

